package ${package}.${moduleName}.${sourceName}MapperSource;

import com.erp.common.MyMapper;
import com.erp.entity.${className}Bean;
import org.apache.ibatis.annotations.Mapper;
import org.springframework.stereotype.Component;
/**
 * ${comments}
 * 
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
@Component
@Mapper
public interface ${className}Mapper extends MyMapper<${className}Bean> {
	
}
